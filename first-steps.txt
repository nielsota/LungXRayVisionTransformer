1. make requirements file to keep track of dependencies

2. make virtual environment: python3 -m venv . (creates bin, include, lib)

3. activate venv: source bin/activate

4. after setting up AWS credentials, run 'python -m pypyr pipelines/data_download' from command line

